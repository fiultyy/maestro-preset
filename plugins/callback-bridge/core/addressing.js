/**
 * addressing.js — 原位 re-export 窄腰库(P4.2,R-B09/D-N1)。
 * 原导出名与签名逐字保留: parseAddress/aliasIndex/resolveRouting(pump 序)。
 * 单一物理源 = plugins/_narrow-waist/addressing.js。
 */
export { parseAddress, aliasIndex, resolveRouting } from '../../_narrow-waist/addressing.js'
