/**
 * store.js — 原位 re-export 窄腰库(P4.2,R-B09)。
 * 原导出名与签名逐字保留: fileSafeSid + createBridgeStore。
 * 单一物理源 = plugins/_narrow-waist/store.js。
 */
export { fileSafeSid, createBridgeStore } from '../../_narrow-waist/store.js'
