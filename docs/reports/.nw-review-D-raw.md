# D路补充评审(F8补全+新发现+verifiedOk)

> 辖区: plugins/orca-callback/pump.js / plugins/message-bridge/index.js / plugins/callback-bridge/。
> 评审对象: docs/narrow-waist-implementation-plan.md。**注意: 该方案文件现为 232 行**(前轮评审时 199 行),本文所有 planRef 按当前 232 行版钉死: §5D 表 = L219-225(pump 行 = L223,fleet-touch 行 = L224,SKILL.md 行 = L225);§6B message-bridge 行 = L207;§2.1 V2_TYPES = L42;§2.3 digestOf = L72;P4 = L147-152。
> 前轮 D-F1..D-F7(0003 复活/零审计/回滚不触达运行面/bridge_http_status 退役/queen-v1 面遗漏/同源行号漂移/行号失准)不重做;§5D"pump 改造(~803→~550行)"与 P4 L149 删目录的矛盾亦为已知,本轮新发现均在其之外。

## D-F8 补全

### D-F8 [建议] §5D pump.js"全保留"清单只点名 4 项,pump 特有机制全清单 27 项中明列 4、间接覆盖 4、**漏 18 项**(其一升级为阻断 D-N1)

- planRef: 方案 L223(pump 行"slots 分槽(incident 0003 防线)、per-consumer 游标、ORCA-CB] 前缀、bridge_arm 注册全保留");间接相关 L194(轮转闸门留宿主 lane)、L229/L231(§7 不变量)。
- evidence: plugins/orca-callback/pump.js 全文 803 行逐行通读,行号如下(全部亲读,非推断):

  | # | pump 特有机制 | pump.js 行号 | 方案涵盖? |
  |---|---|---|---|
  | 1 | slots 分槽(incident 0003 防线) | 683-687, 740-761 | ✅ L223 明列 |
  | 2 | per-consumer 游标 .cursor.\<sid\> | 125-130, 284, 291, 296, 412-427, 539 | ✅ L223 明列 |
  | 3 | ORCA-CB] 前缀 | 694 | ✅ L223 明列;§0 L9/§7 L229 |
  | 4 | bridge_arm 工具注册 | 707-788 | ✅ L223 明列 |
  | 5 | 退避死信: wake 连续失败×3→dead.log + 2s backoff 定时器 | 78-80, 470-479, 619-635(死信落笔 624-631) | ❌ 漏 |
  | 6 | undertaker: 行内在终态唯一死信责任人(armedAt 最早+sid 字典序) | 481-494;调用 571, 591 | ❌ 漏 |
  | 7 | DSH-RE] 回声跳过+归档 echo.log(复制式不重写 inbox) | 86, 441-448, 554-560 | ❌ 漏 |
  | 8 | 幻注册对账(0003 防线第二半): flush 后盘上核验 registry,缺条目→INCONSISTENT 回执 | 767-778 | ❌ 漏 |
  | 9 | registry 写链串行化 registryOpChain(同进程多 pump 撞 tmp 修复) | 196-203;使用 380, 398 | ❌ 漏 → 独立成 D-N1[阻断] |
  | 10 | 游标钳位: 外部轮转/截断使文件变短时 cursor 钳回行尾 | 546-550 | ❌ 漏 |
  | 11 | 轮转闸门: 全部在册消费者到尾才轮转+轮转后归零全部游标 | 496-532(闸门 511-516, 归零 523-527) | △ 不在 L223 清单;L194/L231 间接 |
  | 12 | senders 统计(每 from 投递计数/时间) | 325, 450-461 | ❌ 漏 |
  | 13 | at-least-once 游标推进语义(wake 成功或行终态才推进,失败游标不动) | 48(契约), 632-634, 641-643 | ❌ 漏 |
  | 14 | 尾部残行不消费 splitCompleteLines | 88-95 | ❌ 漏 |
  | 15 | 空白行跳过(blankCount) | 562-567 | ❌ 漏 |
  | 16 | malformed 行→dead.log(undertaker 门控) | 569-578 | ❌ 漏(并入 #6) |
  | 17 | broadcast to:"\*" 全体各 wake 一次 | 144, 172, 584-588 | △ 随"flush() 路由换窄腰统一函数"隐含,未点名 |
  | 18 | 裸别名歧义死信(2026-08-15 事故防线: 绝不挑边) | 184-191 | △ 同上,未点名该防线 |
  | 19 | registry 幂等续期 + armedAt 取首值保 undertaker 稳定 | 377-378, 541-542 | ❌ 漏 |
  | 20 | registry 落盘失败仍并入内存视图(路由不因 IO 抖动丢自己) | 380-393 | ❌ 漏 |
  | 21 | per-slot fs.watch watcher | 753-761 | △ "slots 分槽"隐含 |
  | 22 | teardown: 逐槽关 watcher + 卸册自身条目(effect 清理) | 390-400(注释), 654-660, 790-800 | ❌ 漏 |
  | 23 | 写失败容错策略: cursor/dead/echo/state 写失败不阻塞投递主路径 | 422-427, 429-439, 441-448, 369-372 | ❌ 漏 |
  | 24 | fileSafeSid(游标文件名安全形式) | 125-130 | ❌ 漏 |
  | 25 | wake 双策略: idle→followup / busy→inject + Object.freeze 消息 | 691-704 | ❌ 漏 |
  | 26 | flush 并发合并(rt.flushing 闸) | 534-536 | ❌ 漏 |
  | 27 | 阈值常量导出+createPump 全参可注入(纯函数结构) | 73-84, 258-282 | ❌ 漏 |

- analysis: 方案"全保留"四项确实是 pump 的身份机制,但 #5-#10 是投递正确性与两次事故(0003/2026-08-15)防线的实质载荷: 退避死信与 at-least-once(#5/#13)是 v3.4 投递语义的核心承诺(pump.js:48-51 头注释);undertaker(#6)防止 N 消费者重复死信;回声归档(#7)是 DSH-RE] 回声不回灌的充要机制;幻注册对账(#8)与 slots(#1)同属 0003 防线却是两个独立机制(前者是回执侧盘上核验);registryOpChain(#9)直接升级为 D-N1。清单缺项的直接后果: 实施者按"四处换 import 删内联+四项全保留"执行时,不在保留清单里的机制是否存活完全取决于删改手感——尤其 #9 恰好物理位于被删的 registry 内联块内部。
- suggestion: 把"全保留"清单从 4 项扩为按上表逐项点名(至少 #5-#10、#12、#13、#19-#23),并在 §5D pump 行显式声明"flush() 内一切非路由逻辑(终态判定/游标/退避/轮转/钳位/undertaker)逐行保留,只换 resolveRouting 调用点"。

## 新发现

### D-N1 [阻断] "四处与 core/ 同源"前提在 registry 注册路径与 store 写路径两处不成立——"四处换 import 删内联"按字面执行会删掉 v3.6 registry 写链修复,多槽并发回归 ENOENT/丢更新

- planRef: L223("四处换 import 删内联(~803→~550行)")、L197(core/registry.js 行"提炼进库;……sanitize 白名单逻辑不变;原位 re-export"——只字未提写链/串行化/唯一 tmp 名)。
- evidence:
  - pump.js:196-203 `let registryOpChain = Promise.resolve(); function serializeRegistryOp(...)`,注释原文: "v3.6: 同进程多 pump 并发读改写会撞同一 tmp 路径(ENOENT/丢更新)——模块级链串行化全部 registry 写操作(registerSelf/unregisterSelf)"。
  - pump.js:380, 398 registerSelf/unregisterSelf 全部经 serializeRegistryOp。
  - plugins/callback-bridge/core/registry.js:43-47 `writeRegistryAtomic` 用**固定** tmp `${registryPath}.tmp`;:53-63 registerConsumer / :65-77 unregisterConsumer 为裸读改写,**无任何串行化**(host-callback-bridge/core/registry.js 为其自包含副本,diff 已核实同样无链)。
  - 多 pump 同进程正是 slots 机制制造的常态: pump.js:687(每会话独立 pump)、751(createPump per slot)、542(每轮 flush 都 registerSelf)。
- analysis: §6A L197 计划把 core/registry.js "提炼进库+原位 re-export",而该文件(两份副本皆是)不含 v3.6 写链;§5D 让 pump 删内联换 import——被删的 196-239 块**物理上包含** registryOpChain。字面执行后: 同进程两个 armed 会话的 watcher 同时触发 flush→registerSelf(共享库版),两路读改写撞同一 `registry.json.tmp` → ENOENT/丢更新 → registry 丢消费者条目 → 回调被裁 unknown-addressee 进 dead.log。这正是 v3.6 修掉的事故形态,而方案将其无声复活。反向差异同样存在: core/store.js:49-63 有 writeChain+唯一 tmp(`state.json.tmp-${pid}-${seq}`),pump.js:350-373 saveState 是固定 `state.json.tmp` 无链(pump 自身的 state.json 多槽写仍潜伏同型碰撞,换 import 会顺带改变写语义/时序,方案亦未提)。
- suggestion: 方案 §6A registry 行必须增写: 共享库 registry 写路径并入 pump v3.6 写链语义(或统一唯一 tmp 名+进程内串行化),并把"registryOpChain 保留"写进 §5D 全保留清单;否则 pump 行的"换 import 删内联"不可按字面执行。

### D-N2 [建议] §2.3 `digestOf(envelope,mode)` 与 core/dedup.js `digestOf(line,parsed)` 签名及 from-缺失分支不一致,"与 core/dedup.js 逐字兼容"按规格不可实现

- planRef: L72(`digestOf(envelope, mode='v3') // v3: sha256(from\0msgid);v2: sha256(from\0body)——与 core/dedup.js 逐字兼容`)。
- evidence: core/dedup.js:8-17(= pump.js:106-115 逐字): 签名 `digestOf(line, parsed)`;from 缺失/非对象时 `material = line`(整行 sha256),并非 `from\0body`。pump 路由只看 `to`(pump.js:580-588),`from` 从不校验——from 缺失但 to 命中自身的行真实会走到 dedup(pump.js:609)。
- analysis: v2 模式按 L72 规格对 from=null 行将产出与现实现不同的 digest;升级窗口内同一条重投(或跨通道共享实例,host-callback-bridge/core/dedup.js:24-25"跨 source 共享同一实例")不会命中去重。规格与"逐字兼容"承诺直接冲突。
- suggestion: §2.3 digestOf v2 分支明确写出整行回退语义(`from==null → sha256(line)`),或声明新库 digestOf 仅服务 v3、v2 digest 由 legacy 路径自查。

### D-N3 [建议] §6B"TYPES 改共享常量"无匹配常量: 全方案唯一 type 常量 V2_TYPES(7 值)不含 status、反多 pong/ask/steer/nack——照做即改行为

- planRef: L207(message-bridge 行" TYPES 改共享常量")、L42(V2_TYPES = ['ping','pong','done','ask','steer','nack','ack'])。
- evidence: message-bridge/index.js:73 `TYPES = ['ack','done','ping','status']`;validate 215-217 按此白名单 400;`status` 是现役契约(cb-send:7 用法注释 `type: ack|done|ping|status`;bridge_http_status 工具描述 message-bridge:480 明列 ping/status)。§2.4 词汇表 17 Signal 含 `status`,但入站白名单需要每面子集常量——方案未定义任何与 4 值集对应的常量。
- analysis: 若"共享常量"取 V2_TYPES: ①合法 type=status 的 HTTP 回调开始 400 → cb-send 降级文件桥(cb-send:21-22),HTTP 快道对 status 静默失效、208 幂等语义对该类型丢失;②pong/ask/steer/nack 由拒收变为可投递唤醒(扩面)。两种读法都改变生产行为,且方案没有第三种常量可选。
- suggestion: §2.x 增设每平面入站白名单常量(如 `DSH_CALLBACK_TYPES=['ack','done','ping','status']`),L207 改指名该常量;或显式声明扩面/收面裁决及对拍断言。

### D-N4 [建议] cb-send v3 升格的 msgid/ver/via/ttl 只在文件通道存活,HTTP 通道被 message-bridge:305 规格性丢弃——双通道分叉方案未说明,P3 对拍断言(L143)按字面必失败

- planRef: L206(cb-send 行"增 msgid(uuid4)与可选 --msgid(重发保号)/--ver 3;HTTP 与文件双通道协议不变")、L143(P3 验证"cb-send 新旧对拍逐字段一致(新版多 msgid/ver/via/ttl)")。
- evidence: message-bridge/index.js:305 `const line = JSON.stringify({ type: payload.type, from: payload.from, to, body: payload.body })`——canonical line 硬编码四键,任何 v3 字段进不了 MSGBR] 唤醒行;validate(210-228)不拒未知键但也不透传;cb-send:15-16 注释自认"HTTP 通道丢弃未知字段,两通道统一此格式"。
- analysis: 升格后同一 cb-send 调用: 文件路径(inbox.log 行)携带 msgid/ver/via/ttl,HTTP 路径(MSGBR] 行)不携带——"两通道统一此格式"被 v3 打破而方案未置一词。后果: HTTP 路唤醒的会话永远拿不到 msgid,§2.3"v3 主路径 msgid 索引"在该路不可用;L143 对拍若覆盖 HTTP 通道则"新版多 msgid/ver/via/ttl"断言必失败。§7 L229"载体一字不动"与 L206"增 msgid"之间的通道差异需要一个明示裁决。
- suggestion: L207 增一句: "HTTP 通道维持 v2 四键 canonical line,msgid/ver/via/ttl 仅文件通道携带;对拍断言按通道分列";或规定 message-bridge:305 透传 v3 键(则 MSGBR] 行格式变化,需改 §7 不变量表述)。

### D-N5 [建议] §6A"consumer 条目增 version 字段"与"sanitize 白名单逻辑不变"自相矛盾: 白名单三键会在每次读改写把 version 剥掉,特性自毁

- planRef: L197("consumer 条目增 version 字段(缺省 null 不影响路由);sanitize 白名单逻辑不变")。
- evidence: core/registry.js:12-25 sanitizeConsumers 白名单仅保留 `{alias,pid,armedAt}`(= pump.js:205-218 逐字);readRegistry:27-41 读时过 sanitize,registerConsumer:53-63 读-改-写全经 readRegistry。
- analysis: 写入侧给 consumer 条目加 version,读取侧白名单原样——任何共享库消费者的下一次读改写(含自身续期)都会把该字段静默清除。两半句在同一个改造单元里互斥,实施者无论先做哪半都会观察到字段消失。
- suggestion: 二选一并写明: ①sanitize 白名单扩为四键(此为白名单逻辑变更,需同步 pump.js:205-218 与两份 core 副本);②放弃 consumer 级 version,沿用顶层 `registry.version`(pump.js:226/core registry.js:33 已有)承载代际。

### D-N6 [建议] P4"增 callback-bridge v4 行"未给 cordis.yml config 形态;无 config 时 DEFAULT_CONFIG 双 source 全启,"两行换一行"的等价性完全押在未成文的默认值上

- planRef: L149(P4"cordis.yml 切行(删 orca-callback/message-bridge 行,增 callback-bridge v4 行)");L151(回滚 = git revert cordis.yml)。
- evidence: agent.cordis.yml:361-362 `- id: orca-callback` / `name: './plugins/orca-callback/pump.js'`;:369-370 `- id: message-bridge` / `name: './plugins/message-bridge/index.js'`——两行均**无 config 键**;:354-359、:364-368 为两段说明注释(P4 切行需一并处理,方案未提)。callback-bridge/index.js:85 `apply(ctx, config)`、:58-74 normalizeConfig 容忍无 config;:37-53 DEFAULT_CONFIG = 双 source(file-inbox+http)、sink.messagePrefix='ORCA-CB]'、engine 阈值 60s/3 次/2s;:77-83 bridgeDir 三级解析(env MAESTRO_BRIDGE > config > ~/.dsh/maestro/bridge)。
- analysis: 技术上零 config 可跑(默认即双 source,顶替两插件叠加面),但方案没裁决 v4 行是零 config 还是带 config(如只启单 source、调 engine 阈值、改 aliasEnv)。config 形态不定 → 切行 diff 范围不定 → L151 的"git revert cordis.yml 即回滚"的可审计性受损;且 DEFAULT_CONFIG 的 http source 是否等价现役 message-bridge(v1.3 的 PORT-R1 sig 旁挂:message-bridge/index.js:509-516 写 http.port.sig——属 D-F4 邻域,不展开)未在本路核实范围内被方案对账。
- suggestion: P4 交付物里给出 v4 行的完整 YAML 字面(含是否带 config、注释块去留),并把"默认双 source 与被删两插件行为面差异表"附进 §4-P4。

### D-N7 [建议] message-bridge pickRecipient 内存槽表路由"留应用层"一句带过: 两表分工的三个依赖细节(v3 字段丢弃/208 回放 meta.id/删除后退化单表)均未说清

- planRef: L207("pickRecipient 内存槽表路由**留应用层**(与 registry.json 两张表分工不变)")。
- evidence: message-bridge/index.js:429(进程级单例 `slots = new Map()`)+430(lastArmedId);:414-422 pickRecipient(显式 to 精确命中/缺省 last-armer 兜底);:412 ROUTE_MISS;:454-459 ROUTE_MISS→404→cb-send 降级文件桥按 registry 路由(cb-send:21-22);:286-303 dedup Map 存 `{deliveredAt, id}`,208 回放携带 `prior.id`(:293-300);头注释 :56-57 明言"HTTP 槽表与泵 registry 是两张路由表"。
- analysis: "留应用层"方向正确,但方案未说清: ①"分工不变"的前提被 D-N4 打破——cb-send 升 v3 后两通道携带的字段集不再一致,分工描述需更新;②dedup 换 createDedupWindow 后,208 回放的 `id` 语义靠 core/dedup.js:40-42 的 `meta` 参数承载,方案未提(不提则实施者可能用 §2.3 L73 的四键签名 `{seen,mark,prune,size}` 漏掉 meta);③P4 删 message-bridge 后 HTTP 槽表消失,cb-send 的 PORT-R1 sig 校验(cb-send:36-43 仅在 sig 文件存在时拦截)自然放行、全量落文件桥——分工退化为单表的事实未在方案任何处画出(与 D-F4 相邻、不重复: D-F4 管退役残留,本条管路由分工描述的完整性)。
- suggestion: L207 扩为三句: 槽表/registry 两表分工图;208 回放 meta 依赖;P4 后 cb-send 降级链终态(纯文件桥)。

## verifiedOk

> 逐条以源码为准;仅列核实为准确的断言。fleet-touch get_entry 一项按前轮结论记为偏差(L224 写 105-118,实在 137-141),不入本节。

### D-VO1 ✓ pump.js 共 803 行

- planRef: L149、L223"(pump.js 803 行)"。
- evidence: `wc -l plugins/orca-callback/pump.js` = 803;read 全文末行 803(`export default { version, inject, apply }`)。

### D-VO2 ✓ "四处与 core/ 同源"中的两处半逐字成立(行号为方案 cited 值漂移,前轮 D-F6 已记,此处核内容)

- planRef: L196(addressing"与 pump.js:127-187 同源")、L197(registry"与 pump.js:189-226 同源")、L198(dedup"与 pump.js:101-110 同源")、L199(store)。
- evidence(内容逐字比对,行号为实际值):
  - dedup: pump.js:106-115 ≡ callback-bridge/core/dedup.js:8-17(签名/整行回退/`\u0000` 材料全同)。
  - addressing 三导出: pump.js:142-148(parseAddress)/151-159(aliasIndex)/171-192(resolveRouting) ≡ callback-bridge/core/addressing.js:15-21/24-32/40-61 逐字。
  - registry 三函数: pump.js:205-218(sanitizeConsumers)/220-233(readRegistry)/235-239(writeRegistryAtomic) ≡ callback-bridge/core/registry.js:12-25/27-41/43-47 逐字。
  - store paths 布局: pump.js:287-297 ≈ core/store.js:23-32(inbox/rotated/dead/echo/state/registry/cursorFor 同构;差异: pump 多自有 `cursor` 键)。
- analysis: 上述内容同源成立。**排除项**(不属 verifiedOk,归 D-N1): registry 注册路径(pump 198-203+376-410 带 v3.6 写链 vs core/registry.js:53-77 无链)与 saveState(pump.js:350-373 vs core/store.js:53-63 链+唯一 tmp)两块内容不等价,方案"四处同源内联"对这两处不成立。

### D-VO3 ✓ message-bridge/index.js:73 TYPES 与 cb-send 的 4 值集逐字一致(同源为约定同源,cb-send 侧无校验代码)

- planRef: L207("TYPES 白名单同源")。
- evidence: message-bridge/index.js:73 `export const TYPES = ['ack', 'done', 'ping', 'status']`;bin/cb-send:7 用法契约注释 `type: ack|done|ping|status`——值集与顺序全同。注: cb-send 本体(28-32)对 type 零校验直接 json.dumps,"白名单"仅存在于 message-bridge 侧;两处为文档约定同源而非共享代码。

### D-VO4 ✓ §5D SKILL.md:50-55 确为 9 MessageType 约定段

- planRef: L225("shared/skills/dais-orchestration/SKILL.md:50-55 | 9 MessageType 约定")。
- evidence: SKILL.md:49 `## Message semantics (read first)`;:51 `- 9 message types; only worker_done and heartbeat have lifecycle side effects`;:53 worker_done body 契约 `{"task_id","dispatch_id","outcome"}`;:55 heartbeat `{"dispatch_id"}`;:55 尾"Other 7 types are store-and-audit only"。50-55 恰覆盖该约定段正文。

### D-VO5 ✓ pump.js 无 type 白名单

- planRef: §2.4/§6D 隐含前提(pump 消费面不裁 type);本项为用户指定核实点。
- evidence: `grep -n "TYPES\|whitelist\|白名单" plugins/orca-callback/pump.js` → 零命中(exit 1)。代码佐证: 消费主链只做 JSON 可解析性(pump.js:98-104, 569-578)与 `to` 寻址(580-588);`type`/`from` 字段从不校验——任意 type 的合法 JSON 行只要 to 命中即 wake。

## 附: 范围外新阻断级问题

无。本轮新发现仅 D-N1 一条阻断,其余 6 条为建议级;未发现 D-F1..D-F7 与既知矛盾之外的其他阻断。
