# OF-005 验收回报单

- **ref**: OF-005 · **执行者**: of005(GM 派发 worker) · **完成时间**: 2026-08-23
- **判定**: PASS
- **代码范围**: `bin/ledger`(改:新增 ticket 子命令族,既有 node/event/review/status/project 五子命令代码零触碰)、`tests/of005-selftest.py`(新)、`tickets.md`(render 真实落盘一次:旧手写票板全文包进 `<!-- hand -->` 保留区,头部加 autogenerated 标记)、`reports/OF-005-report.md`(本单)
- **规范依据**: `pipecat-poc/docs/kg/09-orch-hardening-plan.md` §4 OF-005(验收①–⑤)+ §1 OG 门

## A. selftest 输出原文

```
$ cd /home/yy/.dsh/maestro && python3 tests/of005-selftest.py; echo "EXIT=$?"
OF-005 selftest · ledger=/home/yy/.dsh/maestro/bin/ledger · python=3.12.3
[ ok ] C1_既有子命令回归
[ ok ] C2_add与list基础
[ ok ] C3_状态机全合法迁移
[ ok ] C4_状态机非法迁移拒绝
[ ok ] C5_deps前置与force旁路
[ ok ] C6_dep边与环
[ ok ] C7_lease
[ ok ] C8_并发双进程add
[ ok ] C9_并发六进程压力
[ ok ] C10_render_v4字段
[ ok ] C11_render幂等
[ ok ] C12_hand保留区
[ ok ] C13_回溯查询
OF-005 selftest: 13/13 全绿(exit 0)
EXIT=0
```

全程 temp db(MAESTRO_LEDGER=<tmpdir>),render 一律 `-o <temp>`;live `~/.dsh/maestro/ledger.db` 零写入(render 前后 mtime 不变,`ticket list` 读路径容忍缺表返回空集,`status` 既有行为原样)。

## B. 验收①–⑤逐条勾稽

| # | 验收(规范 §4) | 达成 | 证据 |
|---|---|---|---|
| ① | 状态机全合法迁移可达 + 非法迁移(如 dispatched→merged)拒绝 | [x] | C3:12 条合法迁移全表覆盖(6 票 7 条走查含 done→running 回流、merged→rolled-back),每步落 ticket_events append-only;C4:9 组非法迁移(dispatched→merged/done、running→dispatched、merged→running、终态出边等)全部拒绝且状态不变,未知状态名被 choices 拒 |
| ② | deps 检查:前置未 done 时 `state running` 拒绝(`--force` 旁路并 journal) | [x] | C5:D1/D2 双前置未 done→拒(报出未就绪清单);D1 done 后仅 D2 拦;`--force` 放行且 ticket_events 落 `deps-forced` 行(全程仅一次);`done` 迁移同设门;未入库依赖票(OF-404 镜像场景)同样挡门;C6:自依赖/成环(含经传递边)/重复边拒绝,dep 可指向未入库票 |
| ③ | render 输出含 v4 字段(☐◐☑/路径/验证目标指针)且幂等 | [x] | C10:七状态符号逐票断言(R1☐ R2/R3◐ R4..R7☑),R2 段含 worktree/run/dispatch/session 四路径键+`验证目标指针`+依赖列 `R1(dispatched)`;C11:两次渲染逐字节相等、跨目标文件渲染一致(纯 DB 函数);C12:hand 区逐字节保留、再渲染零漂移 |
| ④ | 并发双写 flock 安全(两进程同刻 add 不丢行) | [x] | C8:双进程同刻 add 全成功不丢行;C9:六进程压力全成功+`PRAGMA integrity_check`=ok;实现=写路径统一 `flock(<db>.lock)` 排他锁内单事务提交 |
| ⑤ | 回溯查询:`ticket list --state blocked --since 24h` SQL 可答 | [x] | C13:30h 旧票被 24h 窗排除、无窗全量、35h/2d 窗含全;`--json` 输出结构化行;`--since` 非法格式与未知 `--state` 明确报错非零 |

## C. OG 门勾稽

- **OG1 兼容不破**:C1 对既有五子命令(project/node/event/review/status)在旧 schema fixture 库上逐一断言输出原文格式零漂移;live 库 `status` 干跑输出原样(OF-001 done/OF-005 dispatched/OF-008 dispatched 旧 node 镜像不受影响);`ticket` 为纯新增子命令,旧调用方零改动通过。
- **OG2 冒烟自带**:selftest 13 用例全 temp 域;另真实落盘冒烟一次(见 D)。
- **OG3 红线继承**:未 commit/push;live ledger.db 零写入(mtime 前后一致);pipecat-poc 仓未动。
- **OG4 台账回写**:本票无 D-xx 直修项(主题 B 结构票);台账 §B 组收口记录由 GM 统一回写(见 E)。
- **OG5 信封只增不改**:不涉信封;ticket_events 为独立 append-only 表,不动既有 events 表。

## D. 真实落盘冒烟(render 转渲染视图)

```
$ cd /home/yy/.dsh/maestro && python3 bin/ledger ticket render
render 0 tickets -> /home/yy/.dsh/maestro/tickets.md
$ git diff --stat tickets.md | tail -1
 1 file changed, 10 insertions(+)
```

- 旧手写票板(SI-001..003 全文)完整包进 `<!-- hand -->` 保留区(grep SI-00×7 全中);
- 新头部含 `AUTOGENERATED` 标记与状态符号图例;auto 区因 live 库尚无 tickets 行显示「暂无票据」——GM 后续以 `ticket add` 录入 OF 票后 render 即出镜像视图(wave0 0.3 预期路径);
- 复渲染幂等(selftest C11/C12 已证,真实文件重跑 `ledger ticket render` 不改内容)。

## E. 偏差与备注

1. **live tickets 表空缺**:本票红线禁写 live db,tickets 表将于 GM 首次 `ticket add` 时由写路径自动建 schema;读路径对缺表旧库返回空集不报错(向后兼容验证于 C13 前的 live 干跑)。
2. **状态机语义扩展(规范字面外、规范意图内)**:`rejected` 从 dispatched/running/blocked/done 均可达(规范只写"终态另含 rejected");`done→running` 回流(rework 场景)与 `merged→rolled-back`(规范明列)入表;终态 rejected/rolled-back 无出边。C3 覆盖率断言:12 条合法迁移全表。
3. **deps 门作用于 running 与 done**:票面只写 `state running`;done 同设门(数据一致性,deps 未齐不应终态完成);`rejected`/`rolled-back` 永不设门(止损路径必须可达)。
4. **render 缺省路径**:任务书要求接受 `-o <path>` 缺省 tickets.md——缺省值按 bin/ledger 自身位置推导(`~/.dsh/maestro/tickets.md`),与 MAESTRO_LEDGER 环境变量解耦(selftest 全程显式 `-o temp`)。
5. **账内旧镜像**:live nodes 表已有 GM 手工镜像(OF-005 dispatched);本票不迁移不改写,node 面与 ticket 面并行至 GM 收口。
PASS;报告:reports/OF-005-report.md;测试:13全绿;台账:主题B 待GM回写;备注:五子命令零变化;真实tickets.md已转渲染视图
